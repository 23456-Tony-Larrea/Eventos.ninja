/* var knex = require('knex')({
    clients: 'mysql',
    connection: {
      host : '127.0.0.1',
      user : 'root',
      password : '',
      database : 'eventos'
    }
  }); */
;

  module.exports ={
    client: 'mysql',
    connection: {
      host : '127.0.0.1',
      user : 'root',
      password : '',
      database : 'eventos'
  }
}